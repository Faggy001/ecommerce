apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "django-ecommerce.fullname" . }}-test-connection"
  labels:
    {{- include "django-ecommerce.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "django-ecommerce.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
